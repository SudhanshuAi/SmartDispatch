# SmartDispatch Guest App

Separate Expo client for guests. Full setup is in the **repo root [`README.md`](../README.md)**.

```bash
npm install
copy .env.example .env   # optional
npx expo start --web
```

Login: `guest001@smartdispatch.local` (backend must be seeded and running on `:8000`).
